# Exact versions on install to prevent unintended upgrades
save-exact=true

# Always run audit on install
audit=true

# Provenance attestation on publish
provenance=true

# Resolve the @datasynx scope from the public npm registry
@datasynx:registry=https://registry.npmjs.org
